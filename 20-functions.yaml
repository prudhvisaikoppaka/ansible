- name: functions
  hosts: local
  connection: local
  vars:
    persons: "Ram, Shiva, Sai, Hanuman"
  tasks:
  - name: check undefined variables
    ansible.builtin.debug:
      msg: "Hello, {{ person | default('Sai') }}"

  - name: print names
    ansible.builtin.debug:
      msg: "Hello {{ persons }}"